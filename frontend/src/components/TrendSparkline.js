import React from "react";

const TrendSparkline = ({ data }) => {
  return (
    <div style={{ width: "100%", height: "40px", background: "#f0f0f0", textAlign: "center", lineHeight: "40px" }}>
      Trend Chart (coming soon)
    </div>
  );
};

export default TrendSparkline;
